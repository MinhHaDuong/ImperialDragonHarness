%erg 0.1
Title: Tracker: implement the adopted memory design v7
Created: 2026-09-10
Author: claude
Blocked-by: 0913
Blocked-by: 0918
Blocked-by: 0925
Blocked-by: 0916

--- log ---
2026-09-10T15:50Z claude created
2026-09-11T07:04Z codex note Realigned to v6 after design-review merge #903; retain IDs, add delivery workpackages 0920–0925, defer optional experiments.

2026-09-11T07:20Z codex note Tightened v6 design coverage and acceptance ownership during planning review; no implementation.
2026-09-11T08:20Z claude note Rebased on design v7: acceptance conditions closed in §12.1 and nomenclature settled in §17. Scope unchanged; no implementation.

2026-09-16T15:55Z claude note author settled where the canonical roots live: the harness project's own memory and the collective memory both stay central in ~/.idh, separated from each other. Recorded in the body with its consequence, that step 3 alone does not close the disjoint-roots row and the relocation out of ~/.claude is the remaining half
2026-09-16T16:05Z claude note ran the three relocation probes with positive controls: the runtime follows a symlinked rules/ and a symlinked CLAUDE.md, both confirmed. The memory probe's own control did not fire, so it is recorded as an invalid probe rather than a negative, with the experiment that would settle it named
2026-09-16T16:21Z claude note round 1 review: section sign restored to the corpus spelling, and the blast-radius figure reconciled -- the named directories account for 94 of about 104, the rest sit in no single category. Siblings 0920 and 0923 still assert the store cannot leave ~/.claude, which this decision reverses; they need a cross-reference before either is picked up
--- body ---

## Context

Design v6 was adopted by PR #903. The former v4 train is superseded by the
[implementation plan](../docs/2026-09-11-memory-implementation-plan.md).
This tracker closes only after the core implementation union is reviewed.
Its dependencies deliberately keep it out of the ready queue until then.

The architecture is one internal Python library with thin CLI/adapters,
project-owned bodies plus a bounded shared snapshot, UUID lifecycle and
orientation/recent/task delivery. Rediscovery is only an opportunity for
generalisation; keep particulars unless explicitly withdrawn. Canonical
decisions stay in existing records. One ownership contract and optimistic Git
concurrency replace runtime modes and global locks.

## Author decision, 2026-09-16: the canonical roots live in `~/.idh`

The design records the disjoint-roots row as **known red**, for one stated
reason: IDH's own canonical store sits inside `~/.claude`, which is a runtime's
native memory root (design §12, citing §13.1 step 3 as the remedy).

Step 3 as written moves *project-owned bodies into their repositories*. That
disperses the per-project corpus, but it does not by itself say where the
harness's own memory and the shared snapshot land. If either stays under
`~/.claude`, the row stays red, because the objection is the location, not the
volume.

The author settled it on 2026-09-16:

- **The harness project's own memory stays central, in `~/.idh`.** The harness
  is a project like any other and its repository is the one being relocated, so
  "project memory in its repository" resolves to `~/.idh` for this one.
- **The collective memory stays central too, in `~/.idh`, kept separate from
  it.** The existing split already has the shape: top-level `memory/` for the
  shared corpus, `projects/<slug>/memory/` for per-project bodies.

Consequence, which is a reading of the decision rather than part of it:
relocating the repository out of `~/.claude` is what turns the disjoint-roots
row green, because `~/.idh` is no runtime's native memory root. Step 3 is
necessary and not sufficient; the relocation is the remaining half, and it
belongs to this programme rather than to a parallel portability effort that
could contradict it.

Two facts worth carrying into whichever ticket executes it:

- **The skill layer already supports relocation.** Probed 2026-09-16: with the
  repository outside `~/.claude`, `adapters/perch.py install claude` creates a
  real symlink and reads `SKILL.md` through it. No code change is owed there.
- **The blast radius is about 104 files** referencing `~/.claude` outside
  `projects/` and `tickets/`. The named directories account for 94 of them --
  29 skills, 27 docs, 19 scripts, 14 tests, 5 adapters -- and the remaining ten
  or so sit in no single category (`memory/`, `bin/`, `rules/README.md`,
  `STATE.md`, `settings.shared.json`, `README.md`). An independent recount gave
  103, so treat the headline as an upper bound of the same order, not a
  measurement: many of these are documentation examples rather than live
  dependencies.

Those three probes were run on 2026-09-16, each with a positive control in a
disposable `HOME` before the symlinked case, and the apparatus itself smoke-
tested before either. Results:

| Layer | Control (real file) | Test (symlink) |
|---|---|---|
| `rules/` | codeword returned | codeword returned |
| `CLAUDE.md` | codeword returned | codeword returned |
| `projects/<slug>/memory/` | **control did not fire** | control did not fire |

**Two of three clear the relocation.** The runtime reads a symlinked `rules/`
directory and a symlinked `CLAUDE.md` and loads both into the session. Neither
constrains the layout.

**The third is an invalid probe, not a negative result.** The control placed a
real `MEMORY.md` at `projects/<slug>/memory/` in a disposable home and the
codeword did not come back, so the probe cannot distinguish "the symlink is not
followed" from "this is not the channel memory arrives through". Recorded as
unknown. The cause is not established; the disposable home carried no
`settings.json` and no project context, either of which may gate the load. IDH
injects nothing here -- no hook in `settings.shared.json` or `hooks/` mentions
memory -- so the loader is the runtime's own.

The experiment that would settle it: repeat the control inside a real project
checkout with the harness's own settings present, changing one variable at a
time until the control fires, and only then introduce the symlink. Until that
control fires, this row stays open and the relocation should not assume either
answer for the memory layer.

## Relevant files

- `docs/2026-09-10-dragon-memory-design.md` — adopted v7, not edited by this plan
- `docs/2026-09-11-memory-implementation-plan.md` — exact ticket/dependency map
- Tickets 0908, 0910–0919 and 0920–0925 — implementation and optional experiments

## Actions

1. Foundation: 0911 and 0917; then legacy compatibility 0908 and admission 0915.
2. Pilot: 0920 → 0910 → 0921 → 0922 → 0923. First acceptance is one project,
   a small shared snapshot and one runtime in a disposable offline clone.
3. Expand through 0924 and 0918 → 0913, with measured readiness before broad activation/retirement. In parallel, develop
   manual editorial review through 0923 → 0916 and independent harvesting through 0923 → 0925. Base editorial workflows can use manual judgments; model-assisted automation is optional.
4. Keep 0912 (automated candidate discovery), 0914 (predicate extensions) and 0919 (quality annotation experiment)
   deferred and outside the core closure gate. Reactivate only on evidence.
5. Rerun the relevant 0918 behavioural scenarios on the completed union, adding accepted generalisation/local-exception and harvested-then-amended-procedure cases once 0916/0925 exist. Check their predeclared margins; early pilot results alone cannot certify these later features.
6. Review the merged union, actual adapter capabilities, migration coverage and
   evaluation results. Record any authorised scope changes; do not quietly omit
   a core child or pretend optional work was implemented.

## Test

The integration assessment must exercise active/inactive identity, generalisation
without source retirement, fixed available snapshots, G/C loading, recent writes,
lexical recall, canonical-owner routing, native-writer isolation and optimistic
conflicts. Behavioural judgments use repeated scenarios, not deterministic
semantic assertions. All substantive fixtures need a known failing control.

## Verification

- [ ] Every core child has a merged completion or an explicitly recorded scope decision.
- [ ] Applicable checks pass on the union; actual runtime smoke evidence is read.
- [ ] First-runtime evaluation and remaining-runtime capability declarations agree.
- [ ] No stale ticket demands mandatory residency, automatic promotion or a new ledger.

## Invariants

- This planning amendment closes no implementation work and writes no runtime code.
- Existing IDs and log entries remain; historical scope is available in Git.
- Decisions are not made by memory scores; shared semantic changes are reviewed.
- Optional experiments do not block a useful or complete baseline.

## Exit criteria

- [ ] Foundation, portable pilot, all target adapters, rollout and editorial/harvesting
      work are integrated and reviewed together.
- [ ] Evaluation records behaviour and cost, including limitations; budget proposals
      are not silently promoted to policy.
- [ ] The implementation plan and v7 accurately describe the delivered contract, or
      any deviation is explicitly adopted through the normal decision process.
- [ ] Final integration review is recorded and only then this tracker is closed.
