%erg 0.1
Title: Consolidate ticket-execution entry points (imagine) — start-ticket / raid / hunt / headless raids
Created: 2026-06-12
Author: HDMX-coding-agent
Closed: Option A delivered via children 0293/0294/0295

--- log ---
2026-06-12T10:40Z orchestrator created from aedist orchestration-night retrospective (2026-06-11/12)
2026-07-12T18:55Z claude note Imagine analysis delivered (nightbeat, read-only): start-ticket already merged into hunt (0220) so Q2 is half-resolved; /hunt already IS the canonical core; the real gap is /raid wrapping it in prose instead of a mechanical Skill(hunt) invocation (the aedist drift mechanism); beat.py's `claude -p` spawn already solves headless (no 4th skill); concrete Q4 bug: hunt step 3 ownership check expects t<id> but Agent worktrees are named agent-<id>, colliding with EnterWorktree's no-double-create guard. Recommendation: Option A (skill-invokes-skill), 2-3 small PRs, entry points stay at 2. Full note: docs/imagine-0251-entry-points-2026-07-12.md. Awaiting author ratification per exit criteria.

2026-07-13T06:44Z note Author ratified Option A, all three PRs (2026-07-13). Exit criterion met: architecture recorded here + docs/imagine-0251-entry-points-2026-07-12.md. Now a tracking ticket; children 0293 (raid PR1), 0294 (hunt PR2), 0295 (stub deletion PR3) filed. Stays open until all children close, per tracking-ticket convention.
2026-07-13T07:25Z note Integration review done (raid 2026-07-13): children 0293 (PR #510), 0294 (PR #512), 0295 (PR #509) all merged and closed; composition verified on main — hunt disable-model-invocation: false, raid Phase 5 mechanical Skill(hunt) invocation, stub deleted, guard tests in place; full suite green (make check exit 0). Entry points: 2 (hunt, raid), per the ratified constraint.
2026-07-13T07:25Z Minh Ha-Duong closed — Option A delivered via children 0293/0294/0295
--- body ---
## Context

An orchestrator session (aedist, night of 2026-06-11/12) spawned nine
headless raid agents via the Agent tool with hand-typed execution
contracts instead of any skill. It worked (8/9 delivered), but the
contract drifted between waves — Wave A prompts lacked the
PYTEST_ADDOPTS workaround that Wave B had, and every agent rediscovered
it — demonstrating that the ticket-execution discipline currently
lives in prompt-writing memory, not in a versioned artifact.

The obvious patch — a new headless `raid-ticket` skill — would create a
FOURTH entry point next to `/start-ticket` (interactive, this session),
`/raid` (autonomous multi-ticket waves), and `/hunt` (single ticket).
That is the wrong direction by default: three entry points is already
too many. They share the same core contract (verify ticket open on
fresh origin/main, isolated worktree + branch, test-first, disposition
log before --- body ---, PR with **Ticket:** line) and differ only in
wrapper concerns (interactive vs headless, one ticket vs waves, who
merges).

## Intent (imagine — NOT a handoff execution task)

Rethink the entry-point architecture before adding anything. Questions
the imagine step must answer:

1. Can the shared execution contract be factored into ONE canonical
   core (a skill the others invoke, or a contract document the wrappers
   reference) so the wrappers shrink to their genuine differences?
2. Should some existing entry points merge or die? (e.g. is /hunt a
   degenerate /raid wave of size 1? Is /start-ticket just the
   interactive wrapper of the same core?)
3. How does a headless spawned agent get the contract — Skill
   invocation from inside the agent, or prompt assembled by the spawner
   from the canonical core?
4. Worktree ownership: the core must NOT create a worktree when the
   spawner already provided one (Agent isolation:"worktree") — the
   aedist session hit nested-worktree/cwd confusion twice.

Constraint: fewer or equal entry points after the redesign, never more.

## Decision (2026-07-13)

Author ratified **Option A — skill-invokes-skill**, all three PRs, per
`docs/imagine-0251-entry-points-2026-07-12.md`: `/hunt` stays the single
canonical execution core; every path invokes it mechanically (in-process
`Skill(hunt)`, headless `claude -p "/hunt <id>"` as beat.py already
does). Entry points end at 2 (hunt, raid).

This is now a tracking ticket. Children (one per PR):

- 0293 — raid Phase 5: invoke /hunt mechanically, not by prose paraphrase
- 0294 — hunt step 3: generalize worktree-ownership predicate for
  spawned agents
- 0295 — delete the start-ticket deprecation stub

## Exit criteria
- [x] Imagine discussion held with the author; chosen architecture
      recorded (ADR or ticket log) before any execution sub-tickets
      are filed.
- [ ] All children (0293, 0294, 0295) closed; integration review done.
