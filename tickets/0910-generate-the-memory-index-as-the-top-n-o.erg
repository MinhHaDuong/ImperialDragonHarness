%erg 0.1
Title: Compile accepted memory into reproducible publication views
Created: 2026-09-10
Author: claude
Blocked-by: 0920

--- log ---
2026-09-10T15:50Z claude created
2026-09-11T07:04Z codex note Realigned to design v6 adopted by PR #903; prior scope retained in Git history.

--- body ---

## Context

Own deterministic compilation, not editorial dreaming. Compile the fixed available local/shared pool into a complete catalogue, default orientation and full static candidate order. Pending proposals do not block publication.

Design baseline: v6, merged by PR #903. See `docs/2026-09-10-dragon-memory-design.md` and `docs/2026-09-11-memory-implementation-plan.md`. This planning change implements nothing and closes no work.

## Relevant files

Internal package/CLI from 0911; pilot inputs from 0920; `skills/dream/read-index.py`; `tests/test_resident_census.py`.

## Actions

1. Deduplicate available replicas by UUID and compile index.md, MEMORY.md and a manifest containing processed revisions, available candidates, policy/context/time inputs and hashes.
2. Use explicit simple ranking defaults and stable tie-breaking; no mandatory entries, independent tier quotas, compulsory quality backfill or prerequisite broad predicate grammar.
3. Support configurable budgets with declared token estimates; 1500/500/2000 remains a calibration proposal, not silently adopted production policy.
4. Publish a coherent generation atomically after optimistic expected-revision checks. A rejected stale publication must not overwrite newer accepted changes.

## Test

Over-budget fixture distinguishes complete catalogue and bounded orientation. Repeat fixed inputs for identical output; change policy/context input explicitly. Inject interrupted publication and conflicting revision; ensure coherent old/new output and rejection respectively.

## Verification

- [ ] Run the focused fixtures and show they detect the relevant failure.
- [ ] Run applicable repository checks; record commands, results and limitations.
- [ ] Confirm the implementation matches this scope and v6, including explicit capability limits.

## Invariants

- Canonical bodies and provenance remain reviewable; memory cannot override decisions.
- No mandatory residency, automatic generalisation, new native-memory modes or policy duplication.
- Use the common library and normal Git integration; no runtime implementation is part of the planning PR.

## Exit criteria

- [ ] Generation reproducible for recorded inputs and available set.
- [ ] Below-cut bodies retained and discoverable; default index is not the store.
- [ ] Pending semantic review does not block compilation; no mixed-generation handover.
