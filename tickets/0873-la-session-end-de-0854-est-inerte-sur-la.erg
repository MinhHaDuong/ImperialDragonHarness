%erg 0.1
Title: Le SessionEnd de 0854 est inerte sur la machine, et la selection KEYS vide prive tous les sieges de leur credential
Created: 2026-09-07
Author: claude

--- log ---
2026-09-07T01:20Z claude created successeur de 0854 : les deux cases de verification laissees vides a la fermeture n'etaient portees par aucun ticket. Cause KEYS isolee dans la meme passe, mesuree et non supposee
2026-09-07T03:45Z claude note Action 3 executee sur ordre de l'auteur pendant que ce ticket etait en attente de porte : ~/.claude/.env porte desormais KEYS=openrouter:OPENROUTER_API_KEY_IDH, forme etroite provider:VAR. Controle a deux bras, valeur jamais lue ni affichee : OPENROUTER_API_KEY_IDH passe de UNSET a SET dans un enfant hermetique, cwd worktree et cwd ~/.claude ; les sept autres jeux de openrouter.env plus ANTHROPIC_API_KEY, ZOTERO_API_KEY, AGENT_GH_TOKEN et HAL_PASSWORD restent unset, donc le retrecissement tient. Action 4 satisfaite par observation le meme soir : les deux sieges CLI ont authentifie et rendu un verdict sur les portes de 813, 816 et 818, apres cinq passes ou ils echouaient tous en echec ouvert. Le corps du ticket reste tel qu'ecrit — il etait exact a l'heure de sa redaction — et les cases correspondantes ne sont pas cochees ici : la sonde en enfant hermetique n'a pas ete rejouee sous la garde de cette session
2026-09-07T03:45Z claude note Effet de bord decouvert en posant cette ligne KEYS : make check devient rouge sur tests/test_guard_cd_primary_repo.sh, dont les enfants epinglaient HOME mais heritaient BASH_ENV. Le test etait vert parce que personne n'avait de selection KEYS. Traite a part, ticket 0874, ferme par PR #818 — pas un defaut de ce ticket-ci, mais son successeur immediat dans l'ordre des causes
2026-09-07T03:45Z claude note Divulgation : la sonde de non-fuite ecrite pour verifier le retrecissement utilisait ${VAR:+SET}${VAR:-unset}, dont la seconde substitution rend la VALEUR quand la variable est posee. La cle OpenRouter est donc apparue en clair dans la transcription de session. C'est exactement le mode d'echec que rules/coding-bash.md decrit — le message d'echec EST la fuite — ecrit dans une sonde dont le seul role etait de ne rien afficher. Sonde corrigee (tester, puis afficher un litteral). L'auteur, prevenu immediatement, a choisi de continuer et de faire tourner la cle le 2026-09-08 ; caviardage de la transcription demande. La correction de la sonde n'annule pas la divulgation : seule la rotation la clot
2026-09-07T07:15Z claude note Cle OpenRouter tournee par l'auteur : EXPIRED_OPENROUTER_API_KEY_IDH archivee selon la convention du fichier, nouvelle cle en place et chargee (verifie en boolean, valeur jamais lue). La valeur divulguee dans la transcription du 2026-09-07 est donc morte, et l'incident est clos ; le caviardage de la transcription reste demande mais n'est plus la mesure qui protege. La lecon est en memoire (feedback_boolean_probe_must_not_expand_the_value), pas ici
2026-09-08T06:35Z claude note settings.json vif corrige par l'auteur : SessionEnd -> $HOME/.claude/scripts/on-end.sh, present et identique dans les deux fichiers. Balayage molt passe sur la vraie racine, ce que la nuit precedente ne permettait pas (trois sessions y travaillaient) : quatre orphelins reels retires, huit sessions vivantes epargnees, repertoires non-session intacts. La seule case encore ouverte attend un evenement et non un correctif : aucune session n'est sortie proprement depuis le cablage. Correction d'une erreur de mesure au passage : j'ai d'abord annonce le repertoire de la session precedente comme disparu, artefact d'un head -12 sur un listing trie ; le controle a montre qu'il etait la

2026-09-23T19:30Z Codex note Roster audit: settings.json and settings.shared.json still both point SessionEnd to scripts/on-end.sh. This audit found no identified normal-session exit with before/after scratch-directory evidence, so that verification remains open. Settings drift/reconciliation is owned by ready ticket 0886; do not close 0873 merely from the configured hook or a manual molt cleanup. Credential and CLI-seat observations above remain valid; merged 0393 also improved fail-open visibility.
2026-09-23T19:39Z Codex note Controlled live SessionEnd probe found the cause: Claude set CLAUDE_CODE_CHILD_SESSION in the main-thread hook process, so on-end.sh's old guard returned success without deleting the session directory. Default-root session 910f3da9 exited cleanly with the directory still present; manual invocation removed it. A candidate hook selected by temporary additional settings used the payload's agent_id to guard true subagents and removed controlled session aa553609's real scratch directory on interactive /exit. The fixture suite now covers both cases. Merge the fix, then verify the normal configured hook without extra settings before closing this ticket; 0886 owns the separate settings-drift guard.
2026-09-23T19:44Z Codex note PR #992 merged the payload-agent_id hook fix; its nine CI checks passed. The live settings still call $HOME/.claude/scripts/on-end.sh, now byte-identical to merged origin/main. Controlled session bc6c68ef-3bc7-4eb8-bdf5-7bce7816d5eb had /tmp/claude-1001/<cwd-key>/<session-id>/scratchpad while open; after interactive /exit the debug log recorded SessionEnd:prompt_input_exit for that exact configured script with status 0, and the session directory was absent. No model prompt was needed for this final before/after. The unresolved settings-drift guard is explicitly carried by 0886, not silently accepted here. Ticket acceptance complete.
--- body ---
## Context

Deux defauts distincts, meme forme : une configuration *par machine* qu'aucun
depot ne porte, et dont l'absence ne produit aucune erreur — seulement une
fonctionnalite silencieusement inerte.

**(a) Le hook `SessionEnd` de 0854 ne s'execute jamais ici.** Le ticket 0854 a
ete ferme et archive le 2026-09-07 avec deux cases de `## Verification`
volontairement non cochees, toutes deux etant les etapes machine vive. Le hook
est cable dans `settings.shared.json`, suivi par git ; le `settings.json` vif,
qui est le fichier de l'operateur, n'a pas ete touche. Le nettoyage des
repertoires de travail que 0854 existe pour livrer est donc inerte sur doudou.
`check-settings-drift.sh` signale la derive (sortie 0, informatif). Aucun
ticket ouvert ne portait l'etape restante : c'est ce trou que ce ticket comble.

Rappel de l'enjeu, mesure dans 0854 : `/tmp` est un tmpfs de 12 Go en RAM avec
quota par utilisateur ; le remplissage du 2026-09-06 a tue l'outil Bash dans
trois sessions a la fois et s'est termine par un redemarrage. Sonde reelle le
2026-09-07 : 25 repertoires de session, 5 vivants, 20 orphelins.

**(b) `OPENROUTER_API_KEY_IDH` n'atteint aucun shell.** Mesure boolenne le
2026-09-07 (valeur jamais lue ni affichee) :

| contexte | variable |
|---|---|
| `~/.config/keys/openrouter.env` | presente, longueur plausible |
| enfant hermetique, cwd = worktree | UNSET |
| enfant hermetique, cwd = `~/.claude` | UNSET |
| shell courant | UNSET |

Cause isolee, pas devinee : `~/.claude/.env` fait **0 octet** et ne porte
aucune ligne `KEYS=`. `scripts/bash-env.sh` est en refus par defaut — « no KEYS
line loads no provider secrets » — donc la variable n'est exportee nulle part,
quel que soit le repertoire courant. Ce n'est ni un caprice des shells
d'arriere-plan ni un phenomene intermittent.

Cout constate : les cinq passes de `/gaze` de la nuit du 2026-09-06 au 07 ont
toutes perdu leurs sieges CLI externes en echec ouvert (`--credential-env
OPENROUTER_API_KEY_IDH names an empty or unset variable`). Cinq verdicts rendus
sans aucune decorrelation externe, chacun signalant l'echec sans que personne
ne remonte a la cause. La regle « une verification dont le tout-va-bien ne se
distingue pas du je-n-ai-pas-pu-regarder n'est pas une verification »
s'applique au panel lui-meme.

## Relevant files

- `settings.json` — le fichier vif, par machine, non suivi
- `settings.shared.json` — la version suivie ou 0854 a cable `SessionEnd`
- `scripts/check-settings-drift.sh` — signale la derive, sortie 0
- `~/.claude/.env` — vide ; git-ignore (`.gitignore:2`), donc jamais livre par un depot
- `scripts/bash-env.sh` — la selection `KEYS=` en refus par defaut
- `skills/reviewers/reviewers.sh` — le consommateur des sieges
- `tickets/closed/0854-...erg` — les deux cases laissees vides
- `tickets/0393-...erg` — la resolution keystore, correctif structurel de (b)

## Actions

1. Aligner le `settings.json` vif sur `settings.shared.json` pour l'entree
   `SessionEnd`, puis verifier sur une vraie sortie de session que le
   repertoire de la session disparait.
2. Passer un balayage molt reel contre `/tmp/claude-1001` une fois qu'aucune
   session concurrente n'y travaille, et confirmer qu'il retire un orphelin
   plante et rien d'autre.
3. Ecrire la ligne `KEYS=` de `~/.claude/.env` couvrant le fournisseur
   openrouter. Fichier de la machine, decision de l'operateur : ce ticket
   demande l'acte, il ne le prescrit pas dans le depot.
4. Verifier ensuite qu'un siege CLI repond vraiment sur une passe de `/gaze`,
   plutot que de conclure sur l'absence de l'avertissement.
5. Decider si l'echec ouvert d'un siege doit rester silencieux pour l'appelant.
   Question de contrat entre 0870 et 0393, remontee a l'auteur ; ce ticket la
   porte comme observation, il ne la tranche pas.

## Test

- Derive : une garde qui compare l'ensemble des entrees de hook de
  `settings.shared.json` a celles du `settings.json` vif et nomme les manquantes.
  Controle positif obligatoire : la faire tourner sur l'etat actuel, ou elle
  doit nommer `SessionEnd`, avant de la croire silencieuse ailleurs.
- Credential : une sonde boolenne, jamais la valeur, qui affirme qu'un enfant
  hermetique voit la variable. Rouge aujourd'hui — c'est le controle positif,
  disponible gratuitement tant que (b) n'est pas repare.

## Verification

- [x] La sonde boolenne passe au vert dans un enfant hermetique (2026-09-07)
- [x] Un siege CLI rend un verdict non vide sur une passe de `/gaze` reelle —
      trois portes consecutives (813, 816, 818) apres cinq passes en echec ouvert
- [x] Apres une sortie de session normale, le repertoire de cette session a
      disparu — observe le 2026-09-23 sur le chemin configure, session
      `bc6c68ef-3bc7-4eb8-bdf5-7bce7816d5eb` ; present pendant la session,
      absent apres `/exit`, avec evenement `SessionEnd` dans le journal de debug
- [x] Un orphelin plante est retire par molt sur la racine vive, et rien
      d'autre — verifie le 2026-09-08 sur la vraie racine, et sur des orphelins
      **reels** plutot que plantes, ce qui est plus fort : les quatre sont
      partis, les huit sessions vivantes ont survecu (dont celle qui lancait le
      balayage), et les repertoires non-session du meme parent
      (`gittest_probe*`, `verify817/`) sont intacts
- [ ] La garde de derive nomme `SessionEnd` avant correction, et se tait apres —
      non construite dans 0873 ; transferee explicitement au ticket 0886,
      qui porte la reconciliation de `settings.json` et son controle positif

## Invariants

- Aucune valeur de credential n'est affichee, journalisee, ni passee en argv —
  y compris dans un message d'echec de test.
- Rien ne supprime un repertoire dont la session a un processus vivant.
- `~/.claude/.env` et `settings.json` restent hors du depot : ce ticket ne
  cherche pas a les y faire entrer.

## Exit criteria

- [x] Le hook `SessionEnd` s'execute reellement sur cette machine, prouve par
      une sortie de session et non par la lecture d'un fichier de configuration
- [x] Les sieges externes du panel repondent, prouve par un verdict rendu
- [x] Les deux cases laissees vides a la fermeture de 0854 sont soit satisfaites
      soit reportees ici avec leur raison
- [x] `make check` passe — CI du PR #992, neuf controles verts
