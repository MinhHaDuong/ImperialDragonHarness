%erg 0.1
Title: reviewers: le siège OpenRouter échoue silencieusement — les clés de ~/.config/keys ne sont pas dans l'env des sous-processus
Created: 2026-08-13
Author: Minh Ha-Duong

--- log ---
2026-08-13T19:36Z Minh Ha-Duong created
2026-08-14T12:58Z claude-exec note cause etablie : le fichier de cles n'a aucun export, donc rien n'atteint un sous-processus ; contournement verifie = sourcer sous set -a. Le fail-open (action 3) reste entier

2026-08-14T14:18Z claude-exec note cause corrigee : bash-env.sh source deja les fichiers de cles sous set -a (0334), donc l'absence d'export n'est pas le defaut operant ; le blocage est la selection KEYS= en refus-par-defaut (aucun KEYS dans ~/.claude/.env ni dans le cwd), donc openrouter.env n'est jamais source. Correctif cote consommateur : reviewers.sh resout la variable depuis le keystore. Action 3 faite : harvest nomme sur stdout les sieges qui n'ont pas revu
--- body ---
## Context
Pendant le gaze de polycentric_activity PR #87 (2026-08-12), le siège
externe OpenRouter du panel a échoué fail-open : « missing
OPENROUTER_API_KEY_IDH credential ». Or la clé existe dans
`~/.config/keys/openrouter.env` (vérifié par l'auteur, 2026-08-13). Le
SKILL.md de reviewers affirme « Keys load via the BASH_ENV path (0207) »,
mais `env | grep -c OPENROUTER` retourne 0 dans les sous-processus Bash
d'une session : la chaîne de chargement des clés ne couvre pas (ou plus)
openrouter.env. Conséquence : les sièges externes de décorrélation ne
tournent jamais, silencieusement — le fail-open masque la panne.

## Cause établie (2026-08-14)

L'action 1 demandait « identifier où la doc 0207 charge les `*.env` et pourquoi
openrouter.env n'y est pas ». Ce n'est pas une question de couverture : le
fichier est bien lu, mais il ne peut rien transmettre.

`~/.config/keys/openrouter.env` contient **7 affectations nues et zéro
`export`**. Une affectation sans `export` reste une variable de shell, pas une
variable d'environnement : elle n'est jamais héritée par un processus fils.
Donc `.` ou `source` sur ce fichier réussit, la variable existe dans le shell
qui a sourcé, et `printenv` la trouve absente dans le moindre sous-processus —
exactement le symptôme observé. Le chemin BASH_ENV n'y peut rien : le défaut
est dans la forme du fichier, pas dans qui le charge.

Correctif vérifié à l'usage pendant le gaze de la MR harnais #726
(2026-08-14) : sourcer sous `set -a`, qui exporte automatiquement toute
variable affectée. Cela laisse le fichier de clés intact — préférable à y
ajouter des `export`, puisque le fichier appartient à l'auteur et que la
robustesse doit vivre du côté consommateur.

Reste entier, et c'est la moitié qui compte : l'action 3. Le siège a échoué
**en s'ouvrant**. Un siège de vérification dont la panne se lit comme un
succès est la pathologie que tout le reste du harnais interdit — un contrôle
dont le « tout va bien » est indiscernable de « je n'ai pas regardé ». Tant
que le fail-open subsiste, corriger la credential ne fait que retarder la
prochaine panne silencieuse. Le siège budget échoue d'ailleurs encore, pour
une autre raison non instruite.

## Relevant files
- `skills/reviewers/panel.yml` — `credential-env: OPENROUTER_API_KEY_IDH`
- `skills/reviewers/reviewers.sh` — threading de --credential-env
- le chemin BASH_ENV (0207) — vérifier ce qu'il charge réellement

## Actions
1. Reproduire : `printenv OPENROUTER_API_KEY_IDH` dans un sous-processus
   de session (attendu absent) ; identifier où la doc 0207 charge les
   `*.env` de ~/.config/keys et pourquoi openrouter.env n'y est pas.
2. Corriger : soit charger les fichiers de clés au chemin BASH_ENV, soit
   faire résoudre `credential-env` par reviewers.sh depuis
   ~/.config/keys/<provider>.env quand la variable est absente de l'env
   (comme le fait scripts/zotero-import.py resolve_credentials).
3. Transformer le fail-open en signal : un siège dont la credential ne
   résout pas doit être signalé dans le rapport du panel, pas omis en
   silence.

## Test
- Rouge d'abord : un test qui lance le chemin réel (sous-processus
  hermétique, cf. coding-bash.md § BASH_ENV) et vérifie que la variable
  du siège résout ; il échoue avant correctif.

## Exit criteria
- [ ] Un run de panel avec siège OpenRouter obtient sa credential
- [ ] L'absence de credential produit un avertissement visible, pas un
      silence
- [ ] Test rouge/vert commité
