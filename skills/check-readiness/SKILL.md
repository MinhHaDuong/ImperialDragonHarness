---
name: check-readiness
description: Alias of /scry — multi-repo pre-flight readiness check and interactive triage.
disable-model-invocation: true
user-invocable: true
---

# check-readiness — alias of /scry

Permanent alias (ticket 0220): the descriptive name stays for
discoverability while `/scry` is the canonical name.

Invoke the `scry` skill via the Skill tool, passing along any arguments.
No warning needed — this alias is not deprecated.
