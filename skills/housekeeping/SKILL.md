---
name: housekeeping
description: Alias of /molt — repo housekeeping with git sync, healthcheck, and eager fix-now repairs.
disable-model-invocation: true
user-invocable: true
---

# housekeeping — alias of /molt

Permanent alias (ticket 0220): the descriptive name stays for
discoverability while `/molt` is the canonical name.

Invoke the `molt` skill via the Skill tool, passing along any arguments.
No warning needed — this alias is not deprecated.
