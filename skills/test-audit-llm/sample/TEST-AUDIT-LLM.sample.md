# Test-Audit (read-and-judge) — `ImperialDragonHarness`

> ⚠️ **ADVISORY ONLY — SOFT judgment, not a gate.** These verdicts are a reader's opinion from READING each test (no test was run, no code was mutated). They are NOT a pass/fail oracle and MUST NOT block a merge or fail CI. Use them to FEED ticket creation (`tickets/erg new`), spot-checking each row against the cited evidence before acting. A judge can be wrong; treat a finding as a prompt to look, not a verdict to enforce.

_Generated by the `test-audit-llm` read-and-judge workflow. One judge per test file scored four lenses in a single read; the top 8 flagged/low-confidence files were re-judged by a stronger model. Findings are keyed on the repo-relative test-file path so they compose (as a file-level roll-up) with maw-audit's file identity._

_This is a COMMITTED SAMPLE report (the acceptance anchor for ticket 0183), assembled from canned findings over this repo's own tests plus the smoke fixture — not a live run. Its JSON sidecar (`TEST-AUDIT-LLM.sample.json`) is schema-checked by `tests/test_test_audit_llm_skill.py`._

## Summary

| Lens | Findings |
|---|---|
| 🪞 Faithfulness / test-reality gap (mocks the subject — stays green while prod breaks) | 1 |
| 🪧 Intent legibility (name lies, or passes for the wrong reason) | 1 |
| 🕳️ Negative-space coverage (only the happy path) | 2 |
| 🔁 Change-detector smell (asserts HOW, not WHAT) | 1 |
| **files judged** | 3 / 3 |
| **files escalated to the strong model** | 1 |

## All findings — sorted by severity × risk × churn

| Test file (identity) | Lens | Severity | Confidence | Rationale | Suggestion |
|---|---|---|---|---|---|
| `skills/test-audit-llm/fixtures/fixture_payment_gateway_test.py` | faithfulness | high ⬆️ | high | The test replaces PaymentGateway with a MagicMock, so the real charge() arithmetic and validation never execute. The test stays green even if charge() is gutted. | Instantiate the real PaymentGateway and assert on its return value / ledger; mock only true external boundaries, never the subject under test. |
| `skills/test-audit-llm/fixtures/fixture_payment_gateway_test.py` | intent | high ⬆️ | high | Named test_charge_succeeds but asserts the internal call sequence on a mock; it never checks that a charge succeeded. The name lies about the contract under test. | Rename to test_charge_asserts_call_order (truthful) and replace it with a test that asserts the settled total. |
| `skills/test-audit-llm/fixtures/fixture_payment_gateway_test.py` | negative-space | medium ⬆️ | high | Only the single happy path is exercised. The amount<=0 guard, the over-limit branch, and the short-account boundary in charge() are never tested. | Add cases for amount<=0, amount>limit, and len(account)<3 asserting the ValueError each raises. |
| `skills/test-audit-llm/fixtures/fixture_payment_gateway_test.py` | change-detector | medium ⬆️ | high | Asserts gw.mock_calls equals an exact sequence — it pins the internal call order rather than the observable outcome, and breaks on any harmless refactor. | Assert the observable result (the settled total) instead of the call sequence. |
| `tests/test_git_utils.py` | negative-space | low | low | Illustrative low-confidence finding on a real repo test: the happy-path resolution is covered but the not-a-repo / detached-HEAD branches may be untested. Flagged for strong-model re-judge. | Confirm the error branches are exercised; add a case for a non-repo path if not. |

_⬆️ = re-judged by the stronger model (its verdict replaced the bulk verdict for that file). Sorted by severity × risk × churn so the highest-blast-radius × change-frequency findings surface first; risk is the human-supplied weight (default 1)._

## 🪞 Faithfulness / test-reality gap (mocks the subject — stays green while prod breaks)

| Test file | Severity | Rationale | Suggestion |
|---|---|---|---|
| `skills/test-audit-llm/fixtures/fixture_payment_gateway_test.py` | high | The test replaces PaymentGateway with a MagicMock, so the real charge() arithmetic and validation never execute. The test stays green even if charge() is gutted. | Instantiate the real PaymentGateway and assert on its return value / ledger; mock only true external boundaries, never the subject under test. |

## 🪧 Intent legibility (name lies, or passes for the wrong reason)

| Test file | Severity | Rationale | Suggestion |
|---|---|---|---|
| `skills/test-audit-llm/fixtures/fixture_payment_gateway_test.py` | high | Named test_charge_succeeds but asserts the internal call sequence on a mock; it never checks that a charge succeeded. The name lies about the contract under test. | Rename to test_charge_asserts_call_order (truthful) and replace it with a test that asserts the settled total. |

## 🕳️ Negative-space coverage (only the happy path)

| Test file | Severity | Rationale | Suggestion |
|---|---|---|---|
| `skills/test-audit-llm/fixtures/fixture_payment_gateway_test.py` | medium | Only the single happy path is exercised. The amount<=0 guard, the over-limit branch, and the short-account boundary in charge() are never tested. | Add cases for amount<=0, amount>limit, and len(account)<3 asserting the ValueError each raises. |
| `tests/test_git_utils.py` | low | Illustrative low-confidence finding on a real repo test: the happy-path resolution is covered but the not-a-repo / detached-HEAD branches may be untested. Flagged for strong-model re-judge. | Confirm the error branches are exercised; add a case for a non-repo path if not. |

## 🔁 Change-detector smell (asserts HOW, not WHAT)

| Test file | Severity | Rationale | Suggestion |
|---|---|---|---|
| `skills/test-audit-llm/fixtures/fixture_payment_gateway_test.py` | medium | Asserts gw.mock_calls equals an exact sequence — it pins the internal call order rather than the observable outcome, and breaks on any harmless refactor. | Assert the observable result (the settled total) instead of the call sequence. |

## Per-file summaries

- **`skills/test-audit-llm/fixtures/fixture_payment_gateway_test.py`** (1 faithfulness, 1 intent, 1 negative-space, 1 change-detector) · _strong-model re-judged_ — The known-bad smoke fixture: trips all four lenses by design.
- **`tests/test_git_utils.py`** (1 negative-space) — Real repo test; one low-confidence negative-space flag pending confirmation.
- **`tests/test_skill_descriptions.py`** (clean) — No legible defect on any lens; happy/error space both exercised by the description ratchet.

## Next step (out of scope here)
These are advisory findings, not changes. Triage them into tickets (`tickets/erg new`) — a stronger assertion for an unfaithful test, an error-path case for a negative-space gap, an outcome assertion to replace a change-detector. Spot-check each row against the cited rationale before filing.
