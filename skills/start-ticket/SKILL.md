---
name: start-ticket
description: Deprecated — renamed to /hunt. Warns, then delegates to the new name.
disable-model-invocation: true
user-invocable: true
---

# start-ticket → hunt (renamed)

This skill was renamed in ticket 0220. This stub exists for muscle memory only.

1. FIRST, before any tool call, write this line as plain visible response text (it is the point of this stub — do not skip it): **"Skill start-ticket renamed hunt. Hunting."**
2. Only after that line is emitted, invoke the `hunt` skill via the Skill tool, passing along any arguments.

Do not perform any ticket-start steps here — `/hunt` owns the procedure.
