---
name: Make commands allowed
description: User allows running make commands (manuscript, check, etc.) directly from Claude
type: feedback
---

Running `make` commands (e.g., `make manuscript`, `make check`, `make` in homepage src/) is allowed directly from Claude.

**Why:** User revised earlier preference — running make from Claude is acceptable.

**How to apply:** Go ahead and run make commands when needed as part of the workflow. No need to ask the user to run them separately.
