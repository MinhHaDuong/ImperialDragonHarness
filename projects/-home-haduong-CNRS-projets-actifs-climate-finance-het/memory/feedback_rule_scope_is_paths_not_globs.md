# DELETED 2026-09-25T14:00Z: feedback_rule_scope_is_paths_not_globs
# Reason: DELETE — ENFORCED. The exact blind spot (globs: silently counted as scoped) is now a standing test that treats globs: as unscoped and fails the budget on it.
# Original content preserved in git history.
