# DELETED 2026-09-25T14:00Z: feedback_review_pr_fork_stops_before_synthesis
# Reason: DELETE — ENFORCED/COVERED at the skill level. /review-pr's SKILL.md now documents this exact failure mode and ships the fix (manifest.txt + <perspective>.md.part-then-rename artifact protocol + a bounded wait loop) as the mandatory mechanism, replacing the ad hoc 'read .panel/<pr>/ by hand' workaround this note prescribes.
# Original content preserved in git history.
