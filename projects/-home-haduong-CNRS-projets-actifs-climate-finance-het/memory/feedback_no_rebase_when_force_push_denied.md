# DELETED 2026-07-22T09:20Z: force-push denied → don't rebase a pushed PR branch
# Reason: fully absorbed into ~/.claude/rules/git.md ("Force-push denied → don't rebase a pushed branch; merge as-is"), which cites the same incident (PR #998).
# Original content preserved in git history.
