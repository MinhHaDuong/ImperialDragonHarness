# DELETED 2026-09-25T14:00Z: Pass full DataFrame, not a slice, to functions needing all columns
# Reason: DELETE — STALE and LOW VALUE — the specific bug (ticket 0084) is fixed and closed, and the general lesson ('pass the full DataFrame, not a slice, to a function that needs unlisted columns') is generic pandas engineering judgment, re-derivable.
# Original content preserved in git history.
